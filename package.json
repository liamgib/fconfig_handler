{
  "name": "fconfig_handler",
  "version": "1.0.5",
  "description": "A package that handles configuration data that is sensitive for github or varies across server instances.",
  "main": "dist/index.js",
  "babel": {
    "presets": [
      "es2015",
      "stage-2"
    ]
  },
  "scripts": {
    "prebuild": "rimraf dist",
    "prepublish": "babel --copy-files --out-dir dist --ignore *.test.js src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/liamgib/fconfig_handler.git"
  },
  "keywords": [
    "file",
    "config",
    "handler",
    "sensitive",
    "github",
    "server",
    "instances"
  ],
  "author": "Liam Gibson",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/liamgib/fconfig_handler/issues"
  },
  "homepage": "https://github.com/liamgib/fconfig_handler#readme",
  "dependencies": {
    "read-write-json": "^1.0.5",
    "readline": "^1.3.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-2": "^6.24.1",
    "rimraf": "^2.6.3"
  }
}
